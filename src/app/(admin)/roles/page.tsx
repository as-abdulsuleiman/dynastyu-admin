/** @format */

import { FC } from "react";
import AccountRole from "@/components/account-role";

interface PageProps {}

const Page: FC<PageProps> = ({}) => {
  return <AccountRole />;
};

export default Page;
