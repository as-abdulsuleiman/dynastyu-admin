/** @format */

import { FC } from "react";
import AccountType from "@/components/account-type";

interface PageProps {}

const Page: FC<PageProps> = ({}) => {
  return <AccountType />;
};

export default Page;
