/** @format */

import { FC } from "react";
import AthletePosition from "@/components/athlete-position";

interface PageProps {}

const Page: FC<PageProps> = ({}) => {
  return <AthletePosition />;
};

export default Page;
