/** @format */

import CoachVerificationRequest from "@/components/coach-verification-request";
import { FC } from "react";

interface pageProps {}

const Page: FC<pageProps> = ({}) => {
  return <CoachVerificationRequest />;
};

export default Page;
