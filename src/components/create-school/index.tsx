/** @format */

import { FC } from "react";
import { observer } from "mobx-react-lite";

interface CreateSchoolProps {}

const CreateSchool: FC<CreateSchoolProps> = ({}) => {
  return <div>index</div>;
};

export default observer(CreateSchool);
